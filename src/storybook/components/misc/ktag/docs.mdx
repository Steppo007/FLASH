import { Meta, Description, Canvas, Controls, Story, Title } from '@storybook/addon-docs/blocks';
import * as metaData from './comp.stories';

<Meta of={metaData} />

<Title />

### Custom Component

The `KTag` component displays a status or label, optionally with an icon and a severity level.  
It is styled according to the Krones design system and supports different severities like `success`, `info`, `warning`, `danger`, and more.

<Canvas of={metaData.Story} />
<Controls />

## Example Usage

[comment]: # (START WRITE ./example.tsx)
[comment]: # (DO NOT EDIT: the following has been auto-generated with the command "npm run sync:docs")
```jsx
import Connect from '@flash/ui-components/assets/svgs/products/connect.svg?react';
import { KTag } from '@flash/ui-components/misc/ktag';

export function Example() {
  return (
    <KTag
      severity="success"
      value="Success"
      icon={<Connect />}
      rounded
    />
  );
}
