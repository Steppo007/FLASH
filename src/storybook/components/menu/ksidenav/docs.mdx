import { Meta, Description, Canvas, Controls, Story, Title } from '@storybook/addon-docs/blocks';
import * as metaData from './comp.stories';
import Example from './example';


<Meta of={metaData} />

<Title/>

## Uses: [PanelMenu](https://primereact.org/panelmenu), [TieredMenu](https://primereact.org/tieredmenu), [Sidebar](https://primereact.org/sidebar)

The Side Navigation component should be used as the main routing component
in every FLASH service.

<Example productTitle='Example Application'/>

## How To Build 

[comment]: # (START WRITE ./example.tsx)
[comment]: # (DO NOT EDIT: the following has been auto-generated with the command "npm run sync:docs")
```jsx
import { useState } from 'react';
import KText from '@flash/ui-components/misc/ktext';
import KAppWithSideNav from '@flash/ui-components/misc/kappwithsidenav';
import { ExampleWrapper } from '@sbook/utils';

// the following imports work with vite-plugin-svgr
import LanguageGlobe from '@flash/ui-components/assets/svgs/icons/languageGlobe.svg?react';
import Telephone from '@flash/ui-components/assets/svgs/icons/telephone.svg?react';
import Folder from '@flash/ui-components/assets/svgs/icons/folder.svg?react';
import { KSideNavItem } from '@flash/ui-components/menu/ksidenav';

// if you define the navigation model inside
// your component, we recommend you memo-ize it.
export const navigationModel: KSideNavItem[] = [
  {
    label: 'Files',
    id: 'files',
    icon: <Folder />,
    items: [
      { label: 'Pending', id: 'pending' },
      { label: 'Paid', id: 'paid' },
      { label: 'Clients', id: 'clients' },
      { label: 'Logos', id: 'logos' },
    ],
  },
  {
    label: 'Cloud',
    icon: <LanguageGlobe />,
    id: 'cloud',
    items: [
      { label: 'Upload', id: 'upload' },
      { label: 'Download', id: 'download' },
      { label: 'Sync', id: 'sync' },
    ],
  },
  {
    label: 'Devices',
    icon: <Telephone />,
    id: 'devices',
  },
];

export default function KSideNavExample(props: { productTitle: string }) {
  const [currentPage, setCurrentPage] = useState('pending');

  const Page = (props: { currentPage: string }) => {
    return (
      <div className="flex h-full items-center justify-center">
        <KText>{`Current Page: ${props.currentPage}`}</KText>
      </div>
    );
  };

  return (
    <ExampleWrapper>
      <KAppWithSideNav
        currentPage={currentPage}
        onPageChange={setCurrentPage}
        navigationModel={navigationModel}
        productTitle={props.productTitle}
        className="border-border-default h-96 w-120 border"
      >
        <Page currentPage={currentPage} />
      </KAppWithSideNav>
    </ExampleWrapper>
  );
}
```
[comment]: # (END WRITE)