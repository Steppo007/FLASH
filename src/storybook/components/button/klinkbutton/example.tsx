import { KLinkButton } from '@root/src/components/button/klinkbutton';

export default function Example() {
  return (
    <KLinkButton
      label="Krones"
      disabled={false}
      size="small"
      showArrow={true}
    />
  );
}
